image: ocaml/opam2:latest

cache:
  paths:
    - ~/.opam

before_script:
  - sudo apt-get -y install m4 pkg-config libssl-dev libffi-dev
  - opam init
  - eval `opam config env`
  - opam update
  - opam pin -d add disml .

pages:
  stage: deploy
  script:
  - echo 'Nothing to do'
  artifacts:
    paths:
    - _build/default/_doc/_html
  only:
  - master
  - ci-setup